name: depression_monitoring_app
description: Sahana - A smart Sinhala app for depression monitoring and mental health support using AI
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.1.1
  http: ^1.1.0
  record: ^5.1.2
  sensors_plus: ^4.0.2
  permission_handler: ^11.1.0
  path_provider: ^2.1.1
  shared_preferences: ^2.2.2
  # Firebase packages - updated to compatible versions
  firebase_core: ^3.6.0
  firebase_auth: ^5.3.1
  cloud_firestore: ^5.4.3
  firebase_storage: ^12.3.2
  firebase_messaging: ^15.1.3
  # flutter_webrtc: ^0.9.48  # Temporarily disabled due to build issues
  web_socket_channel: ^2.4.0
  universal_html: ^2.2.4
  speech_to_text: ^7.0.0
  flutter_tts: ^4.0.2
  geolocator: ^13.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

dependency_overrides:
  record_linux: ^1.2.1  # Override to fix compatibility issue

flutter:
  uses-material-design: true

